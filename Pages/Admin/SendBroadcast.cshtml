@page
// FixItFred Patch Log — Sprint 25: SEO + Razor Compliance Sweep
// [2024-07-25T00:00:00Z] — Verified & corrected SEO injection, layout structure, and title block.
// FixItFred Patch Log — Sprint 26.3
// [2025-07-25T00:00:00Z] — Migrated legacy alerts to shared _Alert partial (ViewData pattern). Injected _ValidationSummary for model errors.
@using MVP_Core.Models.Admin
@model MVP_Core.Pages.Admin.SendBroadcastModel
@{
    ViewData["Title"] = "Send Broadcast Alert";
    Layout = "/Pages/Shared/_Layout.cshtml";
}
<h2 class="mb-4">Send Broadcast Alert</h2>
<partial name="_ValidationSummary" />
@if (Model.SuccessMessage != null)
{
    ViewData["AlertType"] = "success";
    ViewData["AlertMessage"] = Model.SuccessMessage;
    <partial name="_Alert" />
}
@if (Model.ErrorMessage != null)
{
    ViewData["AlertType"] = "danger";
    ViewData["AlertMessage"] = Model.ErrorMessage;
    <partial name="_Alert" />
}
<form method="post" class="mb-4">
    <div class="mb-3">
        <label class="form-label">Message</label>
        <textarea name="Message" class="form-control" rows="3" required></textarea>
    </div>
    <div class="mb-3">
        <label class="form-label">Expires At (optional)</label>
        <input type="datetime-local" name="ExpiresAt" class="form-control" />
    </div>
    <button type="submit" class="btn btn-danger">Send Broadcast</button>
</form>
